document.addEventListener("DOMContentLoaded", () => { const skillsSection = document.querySelector(".skill"); const skillProgressBars = document.querySelectorAll(".skill-progress-fill"); const animateSkillProgress = () => { skillProgressBars.forEach((bar, index) => { const progress = parseInt(bar.getAttribute("data-progress"), 10);  const animationDuration = 3000;  const delay = index * 400;  bar.style.width = "0%";  bar.style.transition = "none";  const counter = bar.closest(".skills-item").querySelector("data"); counter.textContent = "0%";   bar.offsetWidth;  setTimeout(() => { bar.style.transition = `width ${animationDuration}ms linear`; bar.style.width = `${progress}%`;  let count = 0; const intervalTime = animationDuration / progress;  const interval = setInterval(() => { count++; counter.textContent = `${count}%`;  if (count >= progress) clearInterval(interval);  }, intervalTime); }, delay); }); }; const observer = new IntersectionObserver( (entries) => { entries.forEach((entry) => { if (entry.isIntersecting) { animateSkillProgress(); } }); }, { root: null, threshold: 0.2,  } ); if (skillsSection) observer.observe(skillsSection);});